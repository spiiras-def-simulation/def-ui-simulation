import AreaTypes from './AreaTypes';

const areaColors = new Map([
  [AreaTypes.RADAR_WARFARE_AREA, 'darkorange'],
  [AreaTypes.RADAR_JAMMING_AREA, 'brown'],
  [AreaTypes.IMPASSABLE_AREA, 'purple']
]);

export default areaColors;
