import AreaTypes from './AreaTypes';

const areaColors = new Map([
  [AreaTypes.SCOUTING_AREA, 'red'],
  [AreaTypes.DUMP_AMMO_AREA, 'dimgrey'],
  [AreaTypes.DEPARTURE_AREA, 'green'],
  [AreaTypes.LANDING_AREA, 'black']
]);

export default areaColors;
